# Copy this file to .env.local for local development, and set the same
# variable in your hosting provider's dashboard for production.

# The live production domain (no trailing slash). Drives canonical links,
# sitemap.xml, robots.txt, and Open Graph/structured data — see lib/seo.ts.
NEXT_PUBLIC_SITE_URL=https://www.kasituitioncentre.com
